# Must be an absolute host path shared by user code and Docker-launched
# Bactopia task containers.
UBIO_DATA_ROOT=/absolute/path/to/ubio-autobox-data

# Replace before starting Compose. Do not commit deploy/.env.
POSTGRES_PASSWORD=replace-with-a-long-random-value
